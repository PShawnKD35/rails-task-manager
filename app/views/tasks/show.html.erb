<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<% completed = @task.completed ? "☑️This task is completed" : "🔲This task is not completed yet"%>
<p><%= completed %></p>
<p>
    <%= link_to "Edit", edit_task_path %>
</p>
<%= link_to "Back to list", tasks_path %>